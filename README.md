# next90fullstack
By next 90 days lets learn and build full stack with angular + spring boot


Here is the breakdown of your 2-hour study schedule, looking at what the countdown timer will read at the start and end of each session.

Since your total time (45 + 45 + 30) exactly equals 120 minutes (2 hours), your breakpoints on a countdown timer will look like this:

### Confidence (Frontend) -> Growth (Backend) -> Challenge (DSA).
### **Countdown Timer Breakpoints**
* Switch to **Spring** when the timer hits **1:15**
* Switch to **DSA** when the timer hits **0:30**

